[package]
name = "general-parser"
version = "0.0.1"
edition = "2024"
publish = false

[features]
default = ["to_string"]
nightly = []
to_string = []

[dependencies]
allocator-api2 = "=0.2.21"
bumpalo = { version = "3.19.0", features = ["allocator-api2", "collections"] }
