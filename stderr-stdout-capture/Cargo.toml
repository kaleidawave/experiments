[package]
name = "stderr-stdout-capture"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
path = "lib.rs"

[target.'cfg(unix)'.dependencies]
libc = "0.2.177"

[package.metadata]
test = "cargo test -- --nocapture"
